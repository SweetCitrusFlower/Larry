apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: judge0-postgres-data
  name: judge0-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

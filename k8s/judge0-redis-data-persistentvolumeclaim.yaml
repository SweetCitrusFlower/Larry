apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: judge0-redis-data
  name: judge0-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
